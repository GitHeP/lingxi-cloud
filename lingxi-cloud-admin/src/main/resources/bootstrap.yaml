spring:
  application:
    name: lingxi-cloud-admin
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        username: nacos
        password: nacos
        server-addr: ${spring.cloud.nacos.server-addr}
        file-extension: yaml
      discovery:
        server-addr: ${spring.cloud.nacos.server-addr}

logging:
  level:
    root: debug
